# Tauri native-layer environment variables.
# API keys for Gemini, Lyria, Live, and Spotify live in the root .env
# as VITE_* variables and are consumed by the Vite frontend, not Rust.
# Add any Tauri-specific secrets here if future native commands require them.
